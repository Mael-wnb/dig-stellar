# AA2 — Founder arbitration on the AA0-b catalog (§8)

Date: 2026-08-20 · Decisions recorded verbatim from founder review of
`aa0-breakage-catalog.md` §8. These drive the AA2 sub-lot; AA0 is validated.

1. **Pools table at `<sm`: COLUMN SWAP.** Replace the PROTOCOL column with
   TVL/APY (protocol identity stays visible via the row icon + the filter
   chips above the table). No horizontal scroll, no card refactor, zero
   data-logic change.

2. **Card strips: MIXED.** Portfolio wallet cards stack to a single column.
   Protocol summary cards and dashboard stat tiles KEEP the horizontal
   peek-strip pattern.

3. **Alerts feed header: approved as proposed.** Stack to: title row / filter
   chips row / full-width "+ New rule" button.

4. **Touch targets: approved as proposed.** One shared-controls pass at `<sm`
   raising shared control heights to ≥44px (chips, modal close buttons, bell,
   nav rows) — not per-view tweaks.

5. **Hero TVL curve: approved.** Min-bar-width / area-fill legibility tweak in
   AA2.

Note: the drawer nav-row height bump to ≥44px ships early with AA1 (the drawer
reuses the sidebar rows — catalog §4); the rest of decision 4 lands in AA2.
